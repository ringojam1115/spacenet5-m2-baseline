# ============================================================
# SpaceNet 5 M2 Baseline — requirements.txt
# Python 3.12, Apple M2 (MPS) 向け
# インストール: uv pip install -r requirements.txt
# ============================================================

# --- PyTorch (MPS 対応) ---
# macOS arm64 用の wheel は https://pytorch.org/get-started/locally/ から確認
# pip install 時は下記で OK (arm64 wheel が自動選択される)
torch>=2.2.0
torchvision>=0.17.0

# --- セグメンテーションモデル ---
# ResNet34-UNet を5行で作れる。ImageNet pretrained weights も同梱。
segmentation-models-pytorch>=0.3.3

# --- 画像拡張 (Augmentation) ---
albumentations>=1.4.0

# --- 地理空間データ ---
# rasterio の macOS arm64 wheel は GDAL を同梱しているので brew gdal は不要
rasterio>=1.3.9
geopandas>=0.14.3
shapely>=2.0.4
fiona>=1.9.6      # geopandas の依存、明示的に入れておく
pyproj>=3.6.1     # CRS 変換

# --- 画像処理 ---
scikit-image>=0.22.0   # skeletonize に使う
opencv-python-headless>=4.9.0.80   # headless = GUI なし(macOS でも安定)

# --- グラフ ---
networkx>=3.3

# --- 数値・データ ---
numpy>=1.26.4
pandas>=2.2.2
scipy>=1.13.0

# --- 可視化 ---
matplotlib>=3.8.4
folium>=0.16.0     # 地図上への重ね合わせ

# --- 実験管理 ---
# wandb を使うには事前に `wandb login` が必要
wandb>=0.17.0

# --- ユーティリティ ---
tqdm>=4.66.4
Pillow>=10.3.0

# --- 開発用 ---
jupyter>=1.0.0
ipykernel>=6.29.4
